#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int b;

for (b = 'a'; b <= 'z'; b++)
putchar(b);
putchar('\n');
return (0);
}

